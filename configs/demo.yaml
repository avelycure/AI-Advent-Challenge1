# Заглушка вместо сети. Ключи не нужны, деньги не тратятся, ответы
# формирует локальный DemoClient. Годится для проверки интерфейса и логики.
name: demo
provider: openai
model: gpt-5.4-mini
transport:
  demo: true
  # null — «живая» случайная пауза 1,2–2,2 с, чтобы ожидание было видно.
  # 0 ставят тесты и массовый прогон.
  demo_delay: null
