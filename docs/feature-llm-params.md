# feature/llm-params — параметры генерации во время диалога

Ветка отведена от `main`, поэтому сравнение ниже — разница именно с ним.

## Коротко

В `main` параметры запроса были прошиты в коде. Температура всегда 0.7,
предел длины всегда равен резерву модели, а `stop`, `top_p` и `response_format`
в чате не использовались вовсе. Чтобы попробовать другое значение, нужно было
править исходники и перезапускать программу.

В этой ветке параметры переключаются **прямо во время диалога** командой, и
изменённое действует для всех последующих запросов до сброса.

```
/change_llm_params                                  показать текущие значения
/change_llm_params max_tokens=200 temperature=0.3   изменить
/change_llm_params stop=###КОНЕЦ###|STOP            несколько стоп-строк через |
/reset_llm_params                                   вернуть значения по умолчанию
```

## Что изменилось в поведении

| | Было (`main`) | Стало (эта ветка) |
|---|---|---|
| Температура | 0.7, прошита в коде | задаётся командой, от 0 до 2 |
| Предел длины | резерв модели | задаётся командой, от 1 до 32000 |
| `top_p` | не передавался | задаётся командой |
| `stop` | не передавался | до четырёх строк |
| `response_format` | не передавался | `json_object` или `text` |
| Смена значения | правка кода и перезапуск | команда в диалоге |
| Видно ли, что параметры изменены | негде посмотреть | отдельная строка в счётчиках |
| Обрыв ответа по лимиту | выглядел как странный ответ | явное предупреждение |
| Непринятый провайдером параметр | тихо убирался | предупреждение с именем параметра |

## Три решения, которые стоит объяснить

**Уменьшенный `max_tokens` освобождает контекст.** Бюджет под историю считается
как окно модели минус *действующий* резерв под ответ, а не минус резерв модели.
Поставив `max_tokens=60`, вы увидите, что свободного места стало на четыре
тысячи токенов больше — и это правда, столько его и стало. В `main` эта
величина была константой, и пересчитывать было нечего.

**Обрыв по лимиту показывается явно.** Если провайдер вернул `finish_reason`
равным `length`, значит модель не договорила, а упёрлась в предел. Программа
говорит об этом прямо и подсказывает команду. Без сообщения обрыв на полуслове
читался бы как поведение модели, а не как следствие собственной настройки —
и это стало заметно ровно тогда, когда предел отдали пользователю.

**Непринятый параметр не скрывается.** Провайдеры поддерживают параметры
неодинаково. Клиент убирает то, что провайдер отверг, и повторяет запрос, чтобы
диалог не прервался, но возвращает список убранного, и пользователь видит
предупреждение. Молчание здесь было бы хуже отказа: человек считал бы, что
ограничение действует, хотя оно не действует.

## Чего параметры намеренно не касаются

Служебный запрос за темой диалога живёт по своим правилам: у него свои
`max_tokens` и температура. Иначе установка `max_tokens=20` ломала бы тему,
а `response_format=json_object` превращал бы заголовок в JSON.

## Что выяснилось живой проверкой

| Наблюдение | Провайдер |
|---|---|
| `max_tokens` работает точно, `finish_reason` возвращается как `length` | GigaChat |
| `top_p` действительно сужает разброс | GigaChat |
| `stop` принимается запросом, но не применяется | GigaChat |
| `response_format=json_object` не мешает обрамлению в тройные кавычки | GigaChat |
| `top_k` принимается запросом и молча игнорируется | GigaChat |
| температура принимается до 5 включительно, хотя выше 2 ответ уже мусор | GigaChat |

Про `top_k` стоит сказать отдельно, потому что вопрос «почему его нет» возникает
сразу. Его нет в протоколе OpenAI, на котором работают все четыре провайдера, и
передать его можно только мимо схемы запроса. Запрос при этом проходит без
ошибки — что хуже отказа, потому что выглядит как успех.

Опыт, который это показал: при температуре 2 и шести запросах подряд `top_p=0.01`
схлопнул ответы до одного варианта, а `top_k=1` оставил два, тогда как должен был
дать ровно один. Ручка, которая молча ничего не делает, вреднее отсутствующей,
поэтому `top_k` в список не попал.

Проверка `stop` требует аккуратности. Если попросить перечислить числа, модель
ответит цифрами, и стоп-строка со словом просто не встретится — опыт получится
пустым. Чистая проверка: попросить слова и оборвать на одном из них. Так и
выяснилось, что GigaChat стоп-строку принимает, но не применяет — слово
«четыре» осталось в ответе при `stop=четыре`.

## Что добавилось в коде

Новый файл `llmchat/params.py` — набор параметров, разбор команды с проверкой
диапазонов и сравнение со значениями по умолчанию.

Изменены: `llmchat/session.py` (параметры в сессии, пересчёт бюджета),
`llmchat/client.py` (`top_p`, `stop`, `response_format`, `finish_reason` и
список непринятых параметров), `llmchat/app.py` (две команды и предупреждения),
`llmchat/ui.py` (строка изменённых параметров в счётчиках, справка).

## Ролик

`demo/record_params_demo.py` записывает демонстрацию: **один и тот же вопрос**
задаётся шесть раз, между опытами меняются только параметры. Перед каждым опытом
история очищается — иначе модель видела бы свои прошлые ответы и сравнение
перестало бы быть честным.

| Опыт | Настройки | Что видно |
|---|---|---|
| 1 | `max_tokens=40` | обрыв на полуслове и предупреждение программы |
| 2 | `temperature=0`, вопрос задаётся дважды | оба ответа совпадают почти дословно |
| 3 | `temperature=1.8`, вопрос задаётся дважды | ответы расходятся и по структуре, и по словам |
| 4 | `response_format=json_object` | GigaChat параметр принял, но не применил — проза осталась прозой |
| 5 | тот же вопрос плюс требование формата словами | приходит JSON |

Опыты с температурой намеренно устроены **симметрично**: в обоих вопрос задаётся
дважды. Первая версия ролика вместо этого обещала подписью, что при 1.8 ответ
развалится в набор слов — и в одном из прогонов он не развалился, потому что
температура на то и температура. Показывать надо не конкретный исход, а механизм:
при нуле два ответа совпадают, при 1.8 расходятся. Это верно всегда.

В конце ролика показывается **итоговое сравнение**: настройка слева, начало
реально прозвучавшего ответа справа. Строки собираются с экрана по ходу той же
записи, а не заготавливаются заранее, — иначе таблица могла бы разойтись с тем,
что зритель только что видел.

Эта таблица сразу окупилась. В одном из прогонов строки `temperature=0`
и `temperature=0, повтор` оказались разными, хотя подпись обещала совпадение.
Причина была не в модели: длинный ответ уезжал за верхний край экрана, и во
второй раз снималась его середина вместо начала. Лимит в опытах с температурой
уменьшен до 120 токенов, чтобы ответ помещался целиком, — и строки сошлись.

Последние два опыта стоят рядом намеренно. Порознь каждый вводит в заблуждение:
первый выглядит так, будто формат задать нельзя, второй — будто параметр работает.
Вместе они показывают правду: формат достижим, но задаёт его инструкция, а не
параметр запроса.

Четвёртый опыт получился нагляднее, чем задумывалось: при температуре 1.8
GigaChat начинает связно, а через полторы строки уходит в словесную кашу.
Это лучшая иллюстрация того, что делает температура, какую можно было желать.

Ролик снят в темпе, рассчитанном на человека: набор идёт медленно, после
набранной строки выдерживается пауза перед отправкой, ответ держится на экране,
пока его не рассмотрят. Пояснения зрителю набираются прямо в строке ввода
и стираются, не будучи отправленными, — так ролик получает закадровый
комментарий без звука и монтажа.

```bash
# GigaChat
.venv-demo/bin/python demo/record_params_demo.py --key-file ~/.gigachat-key

# OpenRouter, бесплатная модель MiniMax M3
.venv-demo/bin/python demo/record_params_demo.py \
  --key-file ~/.openrouter-key --provider 6 --model 1 \
  --label "OpenRouter · MiniMax M3" \
  --output ~/Desktop/llm-params-minimax-demo.mp4
```

Рекордер принимает номер провайдера и модели из меню, поэтому один и тот же
сценарий снимается на любом подключённом сервисе. Подписи к опытам намеренно
не обещают конкретный исход: модель стохастична, и обещание вроде «ответ
совпадёт дословно» на одном провайдере сбывается, а на другом нет. Подпись
говорит, на что смотреть, а не что увидеть.

### Что показал тот же сценарий на MiniMax

| Наблюдение | GigaChat | MiniMax M3 |
|---|---|---|
| `max_tokens` режет ответ | да | да |
| `temperature=0` даёт повторяемость | ответы совпали | совпало только начало |
| `response_format=json_object` | принят, не применён | принят, не применён |
| Строгая схема `json_schema` | — | принята, не применена |
| `stop` | принят, не применён | принят, не применён |
| Формат по инструкции в вопросе | JSON в тройных кавычках | чистый JSON без обрамления |

Вывод одинаков для обоих: **формат надёжно задаёт инструкция, а не параметр
запроса**. Ни один из проверенных бесплатных провайдеров не применил
`response_format`, хотя оба его приняли, а MiniMax принял даже строгую схему
`json_schema` и всё равно ответил прозой.

## Как проверить

```bash
./run.sh
/change_llm_params max_tokens=60 temperature=0.1
Расскажи подробно про язык Rust
```

Ответ оборвётся на полуслове, появится предупреждение про лимит, а в счётчиках
внизу будет видна строка с изменёнными параметрами. `/reset_llm_params` вернёт
всё обратно.

Оговорка про YandexGPT: на момент написания доступ к каталогу начал отвечать
`403 Permission denied` — похоже, закончился грант или снята роль сервисного
аккаунта. Это не связано с этой веткой, но проверять параметры на Яндексе
сейчас не получится.
